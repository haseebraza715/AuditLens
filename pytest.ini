[pytest]
addopts = -q
testpaths = tests
pythonpath =
    src
    server
    ui
markers =
    slow: packaging/subprocess checks (excluded from default CI matrix)
    e2e: Playwright HTTP checks against a live uvicorn server (e2e CI job)
